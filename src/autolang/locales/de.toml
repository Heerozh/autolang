"Autolang developer tools." = """
Autolang-Entwicklertools"""
# --------------------
"Could not infer a unique package root from --source. Pass an absolute --locale-dir or narrow --source." = """
Konnte keinen eindeutigen Paketstamm aus --source ableiten. Geben Sie einen absoluten --locale-dir an oder verengen Sie --source."""
# --------------------
"Create new TOML files for specified locales, and collect tt()-wrapped text templates." = """
Erstellen Sie neue TOML-Dateien für angegebene Sprachen und sammeln Sie tt()-umwickelte Textvorlagen."""
# --------------------
"Locale files already exist for {', '.join(conflicting_locale_names)} in {locale_dir}. Re-run with --force to replace them." = """
Locale-Dateien existieren bereits für {', '.join(conflicting_locale_names)} in {locale_dir}. Führen Sie den Befehl mit --force erneut aus, um sie zu ersetzen."""
# --------------------
"Missing API key. Pass --api-key or set TT_API_KEY/OPENAI_API_KEY." = """
API-Schlüssel fehlt. Übergeben Sie --api-key oder setzen Sie TT_API_KEY/OPENAI_API_KEY."""
# --------------------
"Missing cue TOML files for {locale_dir}. Run `tt sync --source {source_path} --locale-dir {locale_dir}` first." = """
Fehlende TOML-Dateien für Hinweise für {locale_dir}. Führen Sie zuerst `tt sync --source {source_path} --locale-dir {locale_dir}` aus."""
# --------------------
"Missing model. Pass --model or set TT_MODEL/OPENAI_MODEL." = """
Modell fehlt. Übergeben Sie --model oder setzen Sie TT_MODEL/OPENAI_MODEL."""
# --------------------
"Multiple package roots matched the extracted templates. Pass an absolute --locale-dir or narrow --source." = """
Mehrere Paketwurzeln stimmten mit den extrahierten Vorlagen überein. Übergeben Sie ein absolutes --locale-dir oder verengen Sie --source."""
# --------------------
"No locale TOML files found in {locale_dir}" = """
Keine Locale-TOML-Dateien in {locale_dir} gefunden"""
# --------------------
"No locale TOML files found in {locale_dir}. Run `tt init --source {source_path} --locale-dir {locale_dir} --locales <locale>...` first." = """
Keine Locale-TOML-Dateien in {locale_dir} gefunden. Führen Sie zuerst `tt init --source {source_path} --locale-dir {locale_dir} --locales <locale>...` aus."""
# --------------------
"No translatable template keys found in {locale_dir}." = """
Keine übersetzbaren Vorlagenschlüssel in {locale_dir} gefunden."""
# --------------------
"Scan project, Sync the tt()-wrapped text, to all locale TOML files." = """
Projekt scannen, den tt()-eingeschlossenen Text synchronisieren, zu allen lokalen TOML-Dateien."""
# --------------------
"Scanned {scanned_files} Python file(s), initialized {len(locale_names)} locale file(s), created {len(entries)} template entry/entries." = """
{fmt.decimal(scanned_files)} Python-Datei(en) gescannt, {fmt.decimal(len(locale_names))} Lokalisierungsdatei(en) initialisiert, {fmt.decimal(len(entries))} Vorlageneintrag/Einträge erstellt."""
# --------------------
"Scanned {scanned_files} Python file(s), synced {len(locale_files)} locale file(s), tracked {len(unique_messages)} template(s), added {total_added_entries} missing entry/entries, reset {total_invalid_placeholder_resets} invalid placeholder translation(s), removed {total_removed_entries} stale entry/entries." = """
MISSING_TRANSLATION"""
# --------------------
"Source path must be a Python file or directory: {source_path}" = """
Der Quellpfad muss eine Python-Datei oder ein Verzeichnis sein: {source_path}"""
# --------------------
"Source path not found: {source_path}" = """
Quellpfad nicht gefunden: {source_path}"""
# --------------------
"Translate the missing text in all locale TOML language files through an OpenAI-compatible API." = """
Übersetzen Sie den fehlenden Text in allen TOML-Sprachdateien über eine OpenAI-kompatible API."""
# --------------------
"Updated {len(target_locales)} locale file(s), translated 0 entry/entries." = """
{fmt.number(len(target_locales))} Lokalisierungsdatei(en) aktualisiert, 0 Eintrag/Einträge übersetzt."""
# --------------------
"Updated {len(target_locales)} locale file(s), translated {total_translated} entry/entries." = """
{fmt.number(len(target_locales))} Lokalisierungsdatei(en) aktualisiert, {fmt.number(total_translated)} Eintrag/Einträge übersetzt."""
# --------------------
"Warning: A 'tests' directory was found during source scanning. Did you select the wrong --source directory? " = """
Warnung: Während des Quellscans wurde ein 'tests'-Verzeichnis gefunden. Haben Sie das falsche --source-Verzeichnis ausgewählt?"""
# --------------------
